import { makeStyles } from "@material-ui/core";

const styles = makeStyles((theme) => ({
    root: {
        maxWidth: 450,
      },
      media: {
        height: 200,
      },
}));


export default styles;